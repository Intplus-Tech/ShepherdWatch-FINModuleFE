"use client"

/**
 * The branch admin schedules training through the same form as everyone else —
 * one endpoint, one set of fields — so this is that modal under the name the
 * branch-admin screens import.
 */
export { default } from "@/components/hr/CreateTrainingEventModal"
