"use client"

/**
 * Finance files loan applications through the same form as the branch admin —
 * same endpoint, same fields, same validation — so this is the one modal under
 * two names rather than two copies drifting apart.
 */
export { default } from "@/components/hr/BranchAdminNewLoanModal"
