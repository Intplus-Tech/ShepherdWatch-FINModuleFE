"use client"

/**
 * The pastor files requisitions through the same form as the branch admin —
 * one endpoint, one set of required fields — so this is that modal under the
 * name the pastor screens import.
 */
export { default } from "@/components/hr/BranchAdminNewRoleRequisitionModal"
